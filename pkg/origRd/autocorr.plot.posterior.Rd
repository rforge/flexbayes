\name{autocorr.plot.posterior}
\alias{autocorr.plot}
\alias{autocorr.plot.posterior}
\title{Plot autocorrelations of the posterior samples}

\usage{autocorr.plot(x, maxVars = 30, ask = F, ...)}

\arguments{
\item{x}{A \code{posterior} object}
\item{maxVars}{Maximum number of parameters to display}
\item{ask}{Whether to prompt the user before showing the next page of plots}
\item{...}{Other arguments to \code{autocorr.plot} in the \code{coda} package}
}

\description{
  For each parameter in \code{x}, plots the autocorrelation of the posterior samples as 
  a function of the lag.  If \code{x} has 
	multiple chains, then each chain is plotted separately.
  The plots are generated by calling the \code{autocorr.plot} function in the \code{coda}
  package.  For more information on the 
	optional arguments to this function, see the \code{autocorr.plot} section
	of the \code{coda} 
	help.  This is available in the Available Help section of the Help menu when 
	\code{coda} is loaded, which occurs when \code{autocorr.plot} is called 
	on an object of class \code{posterior}.
}
\seealso{
\code{\link{autocorr}},
\code{\link{traceplot}}.
}

